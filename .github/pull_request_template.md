## What & why

<!-- What does this change, and which issue / roadmap item does it advance? -->

Closes #

## Checklist

- [ ] `dart format .` clean, `flutter analyze --fatal-infos` clean, `flutter test` green
- [ ] Schema change? New forward-only migration + RLS policies + policy tests included
- [ ] New UI? Semantics labels added; checked with a screen reader
- [ ] No analytics / ad / tracking dependency added
- [ ] Docs updated (`docs/` or `README`) if behaviour or setup changed

## Security-sensitive?

<!-- Auth, RLS, crypto, payments, media handling, or federation? If so, add a
short threat-model note here. Otherwise write "no". -->
